Для запуска требуется наличие Python>=3.4, затем надо выполнить скрипт main.py.

Процесс обработки строки введенной в интерпретатор происходит по следующим этапам
1) выделяются участки строки находящиеся внутри
   одинарных (неэкранированных) кавычек: clip_both_quotes(s)
2) В интервалах находящихся вне одинарных кавычек происходит подставление
   переменных ($var) из словаря переменных окружения: substitute_dollar(s, envs)
3) Повторное определение участков внутри одинарных и двойных кавычек
4) Вне данных участков производится поиск пайплайнов
   и разбиение по ним на блоки (комманды): split_by_pipeline(s)
5) Для каждого бока определяется не является ли он операцией присвоения,
   если является, то словарь переменных окружения пополняется: is_assignment(block)
6) Остальные блоки рабиваются на непосредственно команду и аргументы,
   снимаются внешиние кавычки, удаляются внешние пробелы: block_to_cmd_args(block)
7) команда и аргументы передаются в мультиплексор,
   который выбирает соотвествующий команде класс: multiplexor(cmd)
8) Инициализируется объект класса команды, задается входной и выходной поток
9) Производится парсинг аргументов комманды и ее выполнение
10) Если для комманды нет специального класса, то ей ставится в соответствие дефолтный класс,
    который пытается запустить комманду как внешнюю программу в отдельном процессе
    с заданными потоками ввода-вывода: class Default(Command)
11) Выполнение комманд связано в цепочку, соответствующую пайплайнам,
    выходной потока первой команды передается на вход второй и т.д.